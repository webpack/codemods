# Checkout with LF everywhere so generated fixture output matches on Windows.
* text=auto eol=lf

# CRLF fixtures exercise the editor's EOL detection — keep their bytes as-is.
codemods/*/tests/**/crlf.config.js -text
